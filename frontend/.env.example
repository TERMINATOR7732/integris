# INTEGRIS Frontend Configuration Example
# Optional: Set this only if the backend is deployed to a remote server.
# In local development, leave this empty to use Vite's built-in /api proxy.

# VITE_API_BASE_URL=https://your-backend-api.onrender.com
